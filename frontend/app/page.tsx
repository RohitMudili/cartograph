import { Landing } from "@/components/landing/Landing";

// The brand landing surface (PRODUCT.md: `/` is brand register). Flexes the
// technical wow (the live graph animation) and converts (paste-a-repo CTA).
export default function Home() {
  return <Landing />;
}
